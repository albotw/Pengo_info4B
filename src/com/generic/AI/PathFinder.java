package com.generic.AI;

/**
 * Fonctionnement de l'algo
 *
 * On prend la case de départ
 * On teste les cases adjacentes (nulles, dans les bords de la map et pas dans la pile de parcourues)
 * une fois que l'on a trouvé celle avec la distance à l'arrivée la plus petite
 * On "marque" la case actuelle  [paire d'entier] ou [booléen] ou [pile]
 * on passe la case séléctionnée comme étant la case actuelle et on recommence
 */

public class PathFinder {

}
